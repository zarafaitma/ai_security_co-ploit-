# --- Host-installed Ollama (not containerized) ---
# host.containers.internal is resolved via the extra_hosts: host-gateway
# entry in docker-compose.yml -- do not change this to 127.0.0.1 or a
# LAN IP, both fail under rootless Podman (see networking notes).
OLLAMA_HOST=http://host.containers.internal:11434
